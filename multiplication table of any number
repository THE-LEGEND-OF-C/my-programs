//multiplication table of any number
#include<stdio.h>
int main(){
    int i,x;
    printf("Enter number to get its multiplication table=");
    scanf("%i",&x);
    for(i=1;i<=10;i=i+1)
        printf("%i*%i=%i\n",x,i,x*i);

    return 0;
} 
