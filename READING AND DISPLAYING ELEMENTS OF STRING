//READING AND DISPLAYING ELEMENTS OF STRING
#include<stdio.h>
int main(){
    char str[100] ;
    printf("ENTER STRING OF STRING = ");
    fgets(str,sizeof(str),stdin) ;
    printf("%s",str) ;

    return 0;
}
